{
  "name": "pockettrainer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --quiet",
    "build": "npx @11ty/eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "stripe": "^14.0.0"
  }
}